Build the full application (frontend + backend integration).

1. Build the frontend:
```bash
cd web && npm run build:lib
```

2. Verify the Python package:
```bash
cd lib && uv run blackfish --help
```

Report any build errors and suggest fixes.
